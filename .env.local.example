# Supabase
# Found in your Supabase project: Settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key - server-side only, NEVER expose to the client.
# Used only inside app/api/** route handlers.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Resend - manufacturer application/approval/rejection emails
RESEND_API_KEY=your-resend-api-key

# Anthropic - Phase 4 Module 3's machine format AI analysis, and Phase 5
# Module 4's WhatsApp bot intent classification, translation, and
# image-label reading (lib/whatsapp/{intent-classifier,localize,vision-analyzer}.ts)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Email address that receives new-manufacturer-application notifications.
# Optional - if unset, that notification is skipped.
ADMIN_EMAIL=admin@example.com

# Phase 4 Module 4 - shared pilot RSA private key (PEM) used to sign every
# manufacturer's QR payloads during the pilot (see
# lib/manufacturers/crypto-signer.ts). Generate one locally with:
#   node -e "console.log(require('crypto').generateKeyPairSync('rsa',{modulusLength:2048,privateKeyEncoding:{type:'pkcs8',format:'pem'},publicKeyEncoding:{type:'spki',format:'pem'}}).privateKey)"
# Paste the full PEM (including the BEGIN/END lines) as the value - either
# as real newlines or with them escaped as \n, both are accepted.
VERIGUARD_PILOT_PRIVATE_KEY=your-pilot-rsa-private-key-pem

# Phase 5 Module 1 - Paystack billing. Test-mode keys from
# https://dashboard.paystack.com/#/settings/developers. Checkout runs
# through Paystack's Inline popup (lib/payments/paystack-inline.ts) so it
# opens as an overlay inside the app instead of navigating away to a
# checkout.paystack.com page - that needs both keys below, unlike the
# redirect-based Standard Checkout this replaced.
PAYSTACK_SECRET_KEY=sk_test_your_paystack_secret_key

# Public key for the Inline popup - safe to expose client-side (it's
# designed to be), used by lib/payments/paystack-inline.ts to open the
# checkout overlay.
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_your_paystack_public_key

# Also enter this same webhook URL in the Paystack dashboard
# (https://dashboard.paystack.com/#/settings/developers ->
# Webhook URL: https://your-deployed-domain.example/api/webhooks/paystack) -
# the inline popup still fires a real charge.success event server-side,
# same as Standard Checkout did. No separate webhook secret to configure -
# Paystack signs deliveries with PAYSTACK_SECRET_KEY itself (see
# verifyWebhookSignature).

# Phase 5 Module 2 - protects app/api/cron/reset-usage, which Vercel Cron
# calls monthly (see vercel.json) to reset manufacturer_profiles'
# units_generated_this_month and whatsapp_sessions' monthly_count back to
# zero. Set this to any long random string on Vercel - Vercel automatically
# sends it as "Authorization: Bearer <value>" on its own cron requests once
# an env var with this exact name exists, no extra wiring needed. Not
# required locally unless you're manually testing that route.
CRON_SECRET=a-long-random-string

# Phase 5 Module 4 - name of the WhatsApp message template
# lib/whatsapp/recall-notifier.ts sends for a proactive recall alert.
# Template messages are the only type Meta allows outside an active
# 24-hour customer-service conversation, and Meta requires every template
# to be submitted and approved in Meta Business Manager
# (business.facebook.com -> WhatsApp Manager -> Message Templates) before
# it can be sent - this is a manual step outside the codebase, not
# something that can be done from here. Submit a template named to match
# this value, category "Alert Update", language English (US), with this
# exact body (two variables: product name, then NAFDAC number):
#
#   ⚠️ VeriGuard Safety Alert: {{1}} (NAFDAC {{2}}) has been recalled.
#   Do not use this product. Reply HELP for more information.
#
# Defaults to "product_recall_alert" if unset - only change this if you
# name your approved template something else.
WHATSAPP_RECALL_TEMPLATE_NAME=product_recall_alert

# Phase 4 Module 7 - the base URL actually printed inside every generated
# QR code (see buildVerificationUrl in lib/manufacturers/code-generator.ts)
# and used by the WhatsApp bot's reply links. Set this to your real
# deployed URL (e.g. https://veriguard-one.vercel.app) - if unset, it
# silently falls back to https://veriguard.ng, which won't resolve to this
# app unless you actually own and point that domain here. Any batch
# exported before this is set correctly will have unscannable QR codes.
NEXT_PUBLIC_APP_URL=https://your-deployed-domain.example
